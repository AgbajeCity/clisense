# Pinned to the exact versions this repo is built, tested and deployed against
# (verified 2026-07-26). Previously used open '>=' bounds, which meant a fresh
# `pip install` today could silently resolve different major versions of
# scikit-learn/xgboost/pandas than the ones the benchmark numbers in
# models/benchmark_metrics.json were actually produced with - reproducible in
# theory, not in practice. Bump these deliberately, then re-run
# `python -m app.benchmark` and diff the metrics before committing a bump.
pandas==2.3.3
numpy==2.0.2
scikit-learn==1.6.1
xgboost==2.1.4
matplotlib==3.9.4
joblib==1.5.3
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.4

# Test-only: pytest runs tests/, and FastAPI's TestClient (tests/test_api.py)
# requires httpx. Neither was previously listed here, so `pip install -r
# requirements.txt` followed by the README's own `pytest tests/` step failed
# with "command not found" on a genuinely fresh machine.
pytest==8.4.2
httpx==0.28.1
